<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:RemoteControl.Views"
             xmlns:viewModels="clr-namespace:RemoteControl.ViewModels"
             x:Class="RemoteControl.Views.CowIdPage">
    <ContentPage.BindingContext>
        <viewModels:CowIdViewModel/>
        <!--<binding:DataModel/>-->
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="5*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Frame Grid.RowSpan="3" Style="{StaticResource FrameDark}">
                </Frame>
                <Frame Grid.Row="0" Style="{StaticResource FrameLight}">
                    <StackLayout>
                        <views:TopView/>
                    </StackLayout>
                </Frame>
                <Frame Grid.Row="1" Style="{StaticResource FrameLight}" VerticalOptions="CenterAndExpand">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Row="0" Grid.RowSpan="1" Grid.ColumnSpan="5" Style="{StaticResource LabelTitle}" Text="Enter a new COW"/>
                        <Label Grid.Row="1" Grid.Column="1" Style="{StaticResource LabelSmall}" Text="Cow ID :"/>
                        <Editor x:Name="EdCowId" Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Style="{StaticResource EditorSmall}"
                                Text="{Binding CowId}"/>
                        <Label Grid.Row="2" Grid.Column="1" Style="{StaticResource LabelSmall}" Text="Tag ID:"/>
                        <Editor x:Name="EdTagId" Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="2" Style="{StaticResource EditorSmall}" Placeholder="*should be auto fill from cow tag"
                                Text="{Binding TagId}"/>
                        <Button x:Name="BtnAddCow" Grid.Row="3" Grid.Column="2" Grid.ColumnSpan="2" Style="{StaticResource ButtonLarge}" Text="Add COW" Command="{Binding AddCow}"/>
                    </Grid>
                </Frame>
                <Frame Grid.Row="2" Style="{StaticResource FrameLight}">
                    <StackLayout>
                        <views:BottomView/>
                    </StackLayout>
                </Frame>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>